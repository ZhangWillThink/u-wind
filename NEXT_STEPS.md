# 接下来要做的事情

## 1. 完善后端任务能力

- [x] 新增 `PATCH /api/tasks/:id`，支持更新任务完成状态。
- [x] 新增 `DELETE /api/tasks/:id`，支持删除任务。
- [x] 为任务接口补充分页/筛选参数（例如按完成状态筛选）。

## 2. 增强前端交互体验

- [x] 在页面上提供"标记完成/删除"按钮，并与后端接口联动。
- [x] 增加请求中的空态、错误重试与提交防抖处理。
- [x] 按模块拆分页面逻辑（`components` + `hooks`），降低 `App.tsx` 复杂度。

## 3. 提升可用性与稳定性

- [x] 将任务数据从内存存储迁移到持久化存储（如 SQLite/PostgreSQL）。
- [x] 为后端 services/api 增加单元测试与接口测试。
- [x] 为前端增加关键交互测试（创建任务、列表刷新、错误提示）。
